# 推进路线

仓库按阶段推进，每个阶段由若干模块组成。目前第一阶段已完成，第二阶段进行中。

## 第一阶段 · 从单机到集群（已完成）

把零散的单机操作串成一条可用的链路：一台机器会装服务，三台机器能协同工作，出问题能定位。

- `01-lnmp-cluster` LNMP 环境（Nginx + PHP-FPM）与备份
- `02-nginx-lb` Nginx 七层负载均衡
- `03-ansible-deploy` Ansible 批量部署
- `04-mysql-replication` MySQL 主从复制与复制状态巡检
- `05-monitoring` Prometheus 监控与告警规则

## 第二阶段 · 容器与编排（进行中）

从「管理机器」转向「管理编排」。机器数量上去之后，靠 SSH 和脚本维护不可持续，需要靠声明式的方式管理应用。

- `06-docker-registry` 镜像构建规范与私有仓库
- `07-kubernetes` 单集群部署与日常运维：kubeadm 部署、节点纳管与排空、Pod 状态排查、网络与存储、资源限制
- `08-cicd` 镜像构建到发布的流水线：版本标记、灰度、回滚

## 第三阶段 · 可观测性与稳定性

有指标只是第一步，还要能在故障发生时快速定位，并且知道系统能扛多少。

- `09-logging` 日志集中采集与检索
- `10-alerting` 告警分级、值班处置流程、误报治理
- `11-capacity` 容量评估与压测

## 第四阶段 · 结合业务场景

把运维能力落到具体的业务系统上，而不只是搭一套环境。

- `12-graph-database` 图数据库集群的部署与运维（AtlasGraph / NebulaGraph）
- `13-tools` 运维工具开发（Python）：资源巡检、配置管理、发布平台

## 单个模块的完成标准

1. 能在一台干净的虚拟机上按 README 复现
2. 配置文件和脚本进仓库，敏感参数走环境变量
3. README 说明：这个模块解决什么问题、关键配置为什么这么写、怎么验证
4. 过程中遇到的问题记进 `docs/troubleshooting.md`

不做只贴命令、不给验证方式的模块。
